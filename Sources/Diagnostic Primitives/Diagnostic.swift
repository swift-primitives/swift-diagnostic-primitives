/// Namespace for diagnostic types.
///
/// `Diagnostic` provides semantic severity classification for structured
/// reporting. Presentation concerns (colors, icons, layout) belong in
/// domain-specific packages that consume these primitives.
public enum Diagnostic {}
